services:
  app:
    build: .
    container_name: rop-management-api
    restart: always
    depends_on:
      - postgres
    ports:
      - "${PORT}:${PORT}"
    environment:
      HOST: ${HOST}
      PORT: ${PORT}
      DB_URL: ${DB_URL}
      ISU_API_KEY: ${ISU_API_KEY}
